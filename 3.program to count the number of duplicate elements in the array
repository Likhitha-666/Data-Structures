#include <stdio.h>

int main() {
    int arr[]={2,2,3,4,5,6,4,6,7,3},i,j;
    int size=sizeof(arr)/sizeof(arr[0]);
    int count=0;
    for(i=0;i<size;i++)
    {
        for(j=i+1;j<size;j++)
        {
            if(arr[i]==arr[j])
            count++;
        
            
        }
    }
    
    
    printf("the number of duplicate elements are:%d",count);

    return 0;
}
